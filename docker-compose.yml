version: "3.9"
services:
  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
      - ./models:/models

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - api
